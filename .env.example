# Copier en .env (jamais commité) puis remplir. Voir docs/07 pour le contexte.

# Étape 3 — OCR (Mistral OCR)
MISTRAL_API_KEY=

# Étape 3bis — normalisation du texte OCR (OpenAI GPT-4o, distinct de l'OCR)
OPENAI_API_KEY=

# Étape 4-5 — e-mail (SendGrid)
SENDGRID_API_KEY=
SENDER_EMAIL=
DEBUG_BCC_EMAIL=
RECEIPTAI_EMAIL=

# Optionnel (défaut : backend/data/uploads)
UPLOAD_DIR=
